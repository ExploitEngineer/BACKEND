<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Home Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="w-full flex justify-end">
        <a
          class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block mb-5"
          href="/logout"
          >Logout</a
        >
      </div>
      <h5 class="mb-5">edit your post.</h5>
      <form method="post" action="/update/<%= post._id %>">
        <textarea
          name="content"
          placeholder="What's on your mind?"
          class="w-1/2 block p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md"
        >
<%= post.content %></textarea
        >
        <input
          class="w-40 text-sm cursor-pointer px-3 py-2 bg-yellow-500 block text-black rounded-md mt-2"
          type="submit"
          value="Update Post"
        />
      </form>
    </div>
  </body>
</html>
